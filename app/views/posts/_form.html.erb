<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title%><br><br>
    <%= f.text_area :title, :maxlength => 50%><br><br>
    <%= f.label :content %><br><br>
    <%= f.text_area :content,:maxlength => 150, :size => "30x10"   %>
    <%= f.submit %>
  </div>
<% end %>
