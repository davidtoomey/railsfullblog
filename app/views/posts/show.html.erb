<!DOCTYPE html>
<html>
  <head>
    <title>Show Comments</title>
    <%=stylesheet_link_tag 'show_comments.css'%>
    <link href='http://fonts.googleapis.com/css?family=Arvo' rel='stylesheet' type='text/css'>
    <meta charset="UTF-8">
  </head>
  <body>

    <%= session[:user_id]%>
          <p id="notice"><%= notice %></p>
          <p>
            <strong>User:</strong>
            <%= @user.username %>
          </p>
          <h3>Title: <%=@post.title %> </h3>
          <p>
            <strong>Content:</strong>
            <%= @post.content %>
          </p>
          <p> Comments: </p>
            <% @comments.each do |comment| %>
            
              <ul>
                <li><p><%= comment.user.username unless comment.user.nil? %></p><p><%= comment.body %><p></li>
                
              </ul>
            <%end%>
            <%= link_to 'Edit', edit_post_path(@post) %> |
            <%= link_to 'Back', posts_path %>
          <div class="comment_box">
          <h1> Leave a comment </h1>
          <%= form_for Comment.new, url: {controller: "comments" ,action: "create"}, html: {class: "comment_form"} do |f| %>
            <%= f.text_area :body, size: "60x12" %>
            <%= f.hidden_field :post_id, :value => @post.id %>
            <%= f.submit "Create" %>
          <% end %>
          </div>
  </body>
</html>